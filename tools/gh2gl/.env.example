# GitHub personal access token (needs read:packages + repo scope)
GITHUB_TOKEN=ghp_...

# GitLab personal access token (needs api scope)
GITLAB_TOKEN=glpat-...

# Repos (defaults shown)
GITHUB_REPO=pytgaen/fimod
GITLAB_PROJECT=pytgaen-group/fimod

# GitLab instance (default: https://gitlab.com)
# GITLAB_BASE=https://gitlab.com
