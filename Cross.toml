[target.x86_64-unknown-linux-musl]
pre-build = [
    "apt-get update -q",
    "apt-get install -y python3",
]

[target.aarch64-unknown-linux-musl]
pre-build = [
    "apt-get update -q",
    "apt-get install -y python3",
]
